<!doctype html>
<html class="h-100" lang="{{ metadata.language }}">

  <head>
    {% include 'snippets/head.njk' %}
  </head>

  <body class="h-100">

    {% include 'snippets/navbar-with-side.njk' %}

    <main class="container">
      <div class="row d-flex align-items-start">
        <div class="row d-flex justify-content-center">
            <div class="aside col col-12 col-sm-3 flex-shrink-0 p-2 bg-white">
              {% include 'snippets/sidebar.njk' %}
            </div>
            <div id="main" class="main col col-12 col-sm-9 pb-5">
              {% include 'snippets/maincontent.njk' %}
            </div>
            {# <div id="result" class="text-center mt-4"></div>
            <button id="nextBtn" class="btn btn-primary mt-4">Next</button> #}
        </div>
      </div>
    </main>

    <footer class="container">
      {% include 'snippets/quiz-footer.njk' %}
    </footer>
  </body>

</html>
